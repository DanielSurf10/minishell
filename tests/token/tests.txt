echo "Hello, World!"
ls -l
cat file.txt
mkdir new_directory
rm file.txt
grep "pattern" file.txt
cd /path/to/directory
pwd
chmod +x script.sh
mv file.txt new_directory/
(ls -l | echo a > out && sleep 5) || ls -la | grep a
(ls -l|echo a>out&&sleep 5)||ls -la|grep a
oi|alo|bom dia|ola
alo&&oi||       oi
